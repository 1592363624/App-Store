name: Auto Pack Private Repo On Push

on:
  repository_dispatch:
    types: [pack_trigger]
  workflow_dispatch:

permissions:
  contents: write   # 创建 Release / Tag
  actions: read

jobs:
  pack-specific-repo:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # Step 1：拉取私有仓库
      # -----------------------------
      - name: Clone Triggered Private Repository
        env:
          REPO_NAME: ${{ github.event.client_payload.repo_name }}
          REPO_BRANCH: ${{ github.event.client_payload.repo_branch }}
          GITHUB_USER: ${{ github.event.client_payload.github_user }}
          TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          set -e

          if [ -z "$REPO_NAME" ] || [ -z "$GITHUB_USER" ]; then
            echo "❌ 缺少 repo_name 或 github_user"
            exit 1
          fi

          echo "📦 拉取私有仓库: $GITHUB_USER/$REPO_NAME"
          REPO_URL="https://x-access-token:${TOKEN}@github.com/${GITHUB_USER}/${REPO_NAME}.git"

          git clone --depth=1 "$REPO_URL" target-repo
          cd target-repo
          git checkout "${REPO_BRANCH:-main}"

      # -----------------------------
      # Step 2：安装依赖（多语言）
      # -----------------------------
      - name: Setup Dependencies
        working-directory: ./target-repo
        run: |
          set -e

          if [ -f "package.json" ]; then
            echo "🔧 检测到 Node.js 项目"
          elif [ -f "pom.xml" ]; then
            echo "🔧 检测到 Maven 项目"
            sudo apt-get update
            sudo apt-get install -y maven
          elif [ -f "requirements.txt" ]; then
            echo "🔧 检测到 Python 项目"
            sudo apt-get update
            sudo apt-get install -y python3-pip
            pip3 install -r requirements.txt
            pip3 install pyinstaller
          else
            echo "⚠ 未识别项目类型，跳过依赖安装"
          fi

      - uses: actions/setup-node@v4
        if: hashFiles('target-repo/package.json') != ''
        with:
          node-version: 20

      - name: Install Node Dependencies
        if: hashFiles('target-repo/package.json') != ''
        working-directory: ./target-repo
        run: npm ci || npm install

      # -----------------------------
      # Step 3：构建项目
      # -----------------------------
      - name: Build Project
        working-directory: ./target-repo
        run: |
          set -e

          if [ -f "package.json" ]; then
            echo "🚀 构建 Node.js 项目"
            npm run build
          elif [ -f "pom.xml" ]; then
            echo "🚀 构建 Maven 项目"
            mvn package -DskipTests
          elif [ -f "requirements.txt" ]; then
            if [ -f "main.py" ]; then
              echo "🚀 构建 Python 项目"
              pyinstaller --onefile main.py
            else
              echo "⚠ 未找到 main.py，跳过 Python 构建"
            fi
          else
            echo "⚠ 无构建步骤"
          fi

      # -----------------------------
      # Step 4：收集构建产物
      # -----------------------------
      - name: Collect Build Artifacts
        id: collect
        run: |
          set -e
          ARTIFACT_DIR="$GITHUB_WORKSPACE/build-artifacts"
          mkdir -p "$ARTIFACT_DIR"

          [ -d "./target-repo/dist" ] && cp -R ./target-repo/dist "$ARTIFACT_DIR/"
          [ -d "./target-repo/target" ] && cp -R ./target-repo/target "$ARTIFACT_DIR/"

          if [ "$(ls -A "$ARTIFACT_DIR")" ]; then
            echo "has_artifacts=true" >> $GITHUB_OUTPUT
          else
            echo "has_artifacts=false" >> $GITHUB_OUTPUT
          fi

      - name: Ensure Artifacts Exist
        if: steps.collect.outputs.has_artifacts != 'true'
        run: |
          echo "❌ 未生成任何构建产物"
          exit 1

      # -----------------------------
      # Step 5：上传 Artifact
      # -----------------------------
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        if: steps.collect.outputs.has_artifacts == 'true'
        with:
          name: ${{ github.event.client_payload.repo_name }}-build
          path: build-artifacts/
          retention-days: 7

      # -----------------------------
      # Step 6：生成 Release Tag
      # -----------------------------
      - name: Generate Release Tag
        id: tag
        run: |
          TS=$(date +'%Y%m%d-%H%M%S')-${GITHUB_RUN_NUMBER}
          NAME="${{ github.event.client_payload.repo_name }}"
          [ -z "$NAME" ] && NAME="manual-build"
          echo "tag_name=${NAME}-${TS}" >> $GITHUB_OUTPUT

      # -----------------------------
      # Step 7：创建 Release
      # -----------------------------
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: steps.collect.outputs.has_artifacts == 'true'
        with:
          tag_name: ${{ steps.tag.outputs.tag_name }}
          name: Release ${{ steps.tag.outputs.tag_name }}
          body: |
            Source Repo: ${{ github.event.client_payload.github_user }}/${{ github.event.client_payload.repo_name }}
            Branch: ${{ github.event.client_payload.repo_branch }}
          files: build-artifacts/**
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
